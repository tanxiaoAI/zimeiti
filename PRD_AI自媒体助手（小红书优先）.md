# AI辅助自媒体工具 PRD（小红书优先｜Web SaaS｜个人创作者）

版本：V0.2（根据你反馈更新：热点API/Mock、封面排版、RAG）  
日期：2026-04-07  
作者：SOLO（产品经理）

---

## 1. 背景与问题
个人自媒体创作者在“账号定位 → 选题 → 文案/脚本 → 封面 → 发布 → 复盘”的链路上，主要痛点是：
1) 方法论分散：定位、拆解、选题与结构化写作缺少统一工作台。  
2) 产出效率低：标题/开头/脚本反复试错，缺少可复用模板与调性约束。  
3) 数据与迭代割裂：爆款拆解与复盘无法闭环到下一次选题与创作。  
4) 大模型可控性不足：不同功能需要不同提示词（Prompt），还需要支持个人风格/文件级配置。

## 2. 产品目标（Goals）
**G1**：把“定位-选题-创作-复盘”整合成一套可复用的工作流，显著缩短从想法到可发布内容的时间。  
**G2**：提供“默认可用 + 可深度定制”的提示词与风格体系：每个功能可配置 Prompt，支持账号/项目/文件级个性化配置。  
**G3**：以**小红书**为首期重点平台，深度适配其内容结构（标题、封面、笔记结构、爆款拆解要素）。  

## 3. 非目标（Non-Goals，首期不做/不承诺）
1) 自动发布/自动运营（对接平台发布接口、自动评论私信等）  
2) 完整的团队协作/审批流（可在后续版本扩展）  
3) 复杂投放闭环（广告投放、归因、ROI 等）  

## 4. 目标用户与典型场景
### 4.1 目标用户
**个人自媒体创作者**（1-3 人小团队）：希望更快产出、对标爆款、维持账号调性、提升数据。

### 4.2 典型使用场景（端到端）
1) 创作者进入工作台 → 选择“平台：小红书” → 创建账号/项目  
2) 进行账号定位：输入自身信息/赛道 → 生成定位方案与内容支柱（Content Pillars）  
3) 账号拆解：输入对标账号链接/笔记链接 → 系统抽取爆款结构与要素  
4) 选题：看热点榜单/趋势 → 生成选题池 → 选择一个选题进入创作  
5) 文案：生成标题/开头/正文结构与填充 → 生成封面（图）与封面文案  
6) 发布后导入数据（MVP 手动导入 + 可选 API）→ 复盘报告 → 产出“下一轮选题建议 + 模板优化建议”

## 5. 产品范围与信息架构
### 5.1 模块划分
1) **账号定位**  
   - 账号定位（定位结论、受众、卖点、内容支柱）  
   - 账号拆解（对标/爆款拆解、可复用模板）
2) **选题与创意**  
   - 特定领域热点榜单（小红书优先）  
   - 选题生成（选题池、角度、结构提纲）
3) **文案生成**  
   - 标题生成  
   - 开头生成  
   - 脚本/正文填充  
   - 封面生成（图 + 字）
4) **数据分析**  
   - 账号数据分析  
   - 内容数据分析
5) **提示词与个性化配置（横切能力）**  
   - 每个功能的 Prompt 模板  
   - 文件/项目/账号的个性化配置（风格、禁忌词、结构偏好等）
6) **知识库（RAG）**  
   - 资料上传与管理（品牌/产品/个人素材）  
   - 检索与引用（为选题/文案生成提供依据与风格约束）

### 5.2 核心页面（建议）
1) 登录/注册、模型用量与订阅（如有）  
2) 工作台：账号/项目列表  
3) 账号定位页（定位生成 + 内容支柱管理）  
4) 拆解页（输入链接/文本 → 拆解结果 + 模板沉淀）  
5) 热点榜单页（赛道/关键词筛选、收藏、加入选题池）  
6) 选题池页（选题卡片、评分、状态流转：待创作/创作中/已发布）  
7) 编辑器页（标题/开头/正文/脚本/封面一体）  
8) 数据导入与分析页（账号维度、内容维度、复盘建议）  
9) Prompt 与配置中心（默认模板、版本、个人覆盖、导入导出配置文件）
10) 知识库页（资料上传、标签、检索、引用）

---

## 6. 详细需求（按模块）

> 说明：下文中每个“AI 生成能力”都默认依赖【提示词模板 + 个性化配置 + 模型调用】三部分，具体见第 7 章。

### 6.1 模块一：账号定位

#### 6.1.1 功能：账号定位
**目标**：帮助创作者明确“赛道/人设/受众/内容支柱/差异化卖点/表达风格”，并可复用到后续选题与文案生成中。

**输入**（表单 + 可选文件导入）：
- 平台：小红书（首期固定，可扩展）  
- 赛道/领域（如：护肤、穿搭、母婴、职场…）
- 创作者背景（经历、资源、优势）
- 目标受众画像（年龄、痛点、消费能力、场景）
- 目标（涨粉/引流/变现/品牌心智）
- 风格偏好（理性/温柔/犀利/幽默…）
- 禁忌（敏感词/不写的话题/合规要求）

**输出**（结构化）：
- 定位一句话（Positioning Statement）
- 账号人设（Persona）
- 受众与痛点（Audience & Pain Points）
- 差异化卖点（Differentiators）
- 内容支柱（3-5 个 Content Pillars）与示例选题
- 内容结构建议（小红书笔记结构：标题-开头钩子-正文分点-总结-互动引导）
- 30 天内容规划（可选）

**验收标准**：
1) 输出必须结构化（可复制、可保存为“账号配置”）。  
2) 内容支柱可编辑、可作为后续选题生成的输入变量。  
3) 定位结果可一键“应用到全局配置”（见第 7 章变量体系）。  

#### 6.1.2 功能：账号拆解（对标/爆款拆解）
**目标**：输入对标账号/爆款内容，抽取可复用的“选题模式、结构模板、标题公式、封面风格、互动引导套路”等。

**输入**：
- 对标账号主页链接（可选）  
- 单篇笔记链接/文本（MVP 支持粘贴文本 + 图片OCR/截图可后续扩展）  
- 拆解维度开关：标题/封面/开头钩子/正文结构/情绪价值/证据链/互动引导/标签话题/评论区玩法

**输出**：
- 结构拆解（分段、每段意图、转折点）  
- 标题公式与可替换变量  
- 封面风格建议（配色、字体、排版要点、关键词）  
- 可复用模板（Template）：可保存到“模板库”，供后续创作一键套用  
- 同赛道可复制选题建议（10-20 个）

**边界**：
- 首期不保证抓取受限页面内容；优先做“用户可粘贴的内容 + 结构化拆解”。  
- 若后续接入爬虫/平台开放数据，需要合规评估。  

---

### 6.2 模块二：选题与创意

#### 6.2.1 功能：特定领域热点榜单（小红书优先）
**目标**：为某赛道提供“热词/热榜/趋势话题”，支持收藏与转化为选题卡片。

**输入**：
- 领域/赛道选择（与账号定位绑定）
- 时间范围（24h/7d/30d）
- 关键词订阅（可选）

**输出**：
- 热点列表：话题/关键词、热度指标（指数/热度分）、趋势（上升/下降）、类目、更新时间、**代表内容示例（笔记标题/链接/作者等）**  
- 推荐切入角度：科普/避坑/测评/清单/对比/故事/挑战

**MVP 实现建议**：
- 你将提供**小红书热点榜单 API**：产品侧抽象为统一榜单接口。  
- 开发早期所有依赖 API 的能力（含热点/大模型/分析等）均支持 **Mock 模式**，确保前后端并行与快速联调。  

#### 6.2.2 功能：选题生成（选题池）
**目标**：根据账号定位 + 热点/关键词 + 历史表现，生成可执行的选题池，并给出结构提纲与产出难度。

**输入**：
- 账号定位变量（自动带入）
- 关键词/热点（来自榜单或手填）
- 选题目标：涨粉/转化/种草/品牌心智
- 内容形态：图文/短视频/多平台分发
- 数量、偏好（偏干货/偏情绪/偏故事）

**输出**（选题卡片）：
- 选题标题（工作标题）+ 3 个角度变体  
- 目标受众与核心痛点  
- 笔记结构提纲（小红书）/脚本分镜提纲（短视频）  
- 亮点与差异化建议  
- 素材清单（需要拍哪些图/证据/案例）  
- 预估难度与耗时（低/中/高）  

**验收标准**：
- 选题可一键进入编辑器并自动生成“标题/开头/正文/封面”初稿。  
- 选题卡片有状态：待创作/创作中/已发布/已复盘。  

---

### 6.3 模块三：文案生成

#### 6.3.1 功能：标题生成
**输入**：
- 选题卡片（自动带入）
- 标题偏好：数字/对比/避坑/清单/情绪/悬念
- 平台规则（小红书敏感词、字数建议）

**输出**：
- 20 个标题候选 + 分类标签（风格/公式）  
- 1-3 个“最推荐标题” + 推荐理由（与受众痛点对齐/强利益点/可验证）  
- 可选：标题 A/B 版本（用于多平台分发改写）

#### 6.3.2 功能：开头生成（Hook）
**目标**：生成 3-5 种开头钩子并适配小红书阅读习惯。

**输出**：
- 悬念式/共鸣式/结果先行/反常识/故事式开头各 1-2 个  
- 互动引导一句（评论区提问/投票）  

#### 6.3.3 功能：脚本/正文填充
**目标**：把提纲扩写为可发布的正文/口播脚本，并提供可编辑的结构化块。

**输入**：
- 提纲（来自选题生成或用户自写）
- 证据素材（要点、数据、案例、产品信息）
- 可选：知识库引用范围（选择资料集/标签/是否强制引用）（见 6.5）
- 文风：与账号定位一致（默认从配置中心读取）

**输出**：
- 正文/脚本（分段块：开头/冲突/方法论/案例/总结/CTA）  
- 每段“写作意图”提示（可开关）  
- 多平台改写：同一内容生成小红书/抖音口播/公众号版本（首期可先做“小红书 + 抖音口播”）
- 可选：引用清单（来自知识库的片段/来源标识），支持一键插入到正文（提升可信度与可审计性）

#### 6.3.4 功能：封面生成（图 + 字）
**目标**：以**模板化排版渲染**为主生成小红书风格封面（含封面标题/卖点词），保证一致性；生图作为可选增强能力。

**输入**：
- 标题/核心卖点
- 封面风格：简约/杂志/手账/对比测评/清单
- 品牌色/字体偏好（来自配置）

**输出**：
- 3-6 张封面图（基于模板渲染：版式/字体/配色稳定）  
- 可编辑图层（优先级更高：MVP 建议直接支持“文字可编辑 + 重新渲染”）  
- 封面文案备选（大字标题 + 小字补充）

**依赖**：
- 模板渲染引擎（P0 依赖）：可配置版式、字体、品牌色、字重、边距等  
- 图片生成模型（可选增强）：用于补充背景素材/风格化图（非首要依赖）

---

### 6.5 模块五：知识库（RAG）
**目标**：让创作者把“品牌/产品资料、个人笔记、历史爆款拆解、常用论据/数据”等沉淀为可检索的知识库，在选题与文案生成时自动检索引用，提升一致性与可信度。

#### 6.5.1 功能：资料上传与管理
**输入**：
- 上传文件：PDF/DOCX/TXT/Markdown（MVP 仅支持文档类；**不做图片OCR/表格解析**）  
- 手动粘贴：文本片段/FAQ  
- 元信息：所属账号/项目、标签、资料类型（品牌规范/产品参数/案例/数据/话术）

**输出/能力**：
- 资料列表：状态（待处理/已索引/失败）、标签、更新时间  
- 资料预览与检索（关键字 + 语义检索）  
- 删除/下线：支持“从索引移除”（合规与纠错）

#### 6.5.2 功能：检索与引用（用于生成）
**交互**：
- 在选题生成/脚本填充时选择“引用范围”（默认：当前账号/项目全部资料）  
- 支持“强制引用”：要求输出必须包含 N 条引用依据  

**输出**：
- 检索到的引用片段（TopK）：片段内容 + 来源文件 + 片段定位（页码/段落号，若可用）  
- 生成结果中标注引用（可开关）：例如“依据：资料A-片段2 …”

**验收标准**：
- 同一账号下的不同内容生成，风格与术语一致性明显提升  
- 生成记录中可追溯：本次生成检索了哪些片段（用于审计与复盘）

---

### 6.4 模块四：数据分析

> 你选择了“手动导入 + 平台 API 授权”两种方式：建议 **MVP 先手动导入**，API 作为后续增量。

#### 6.4.1 功能：账号数据分析
**输入**：
- 手动导入：平台后台导出的 CSV/Excel（或用户复制粘贴数据）  
- API 授权：后续接入

**输出**：
- 增长趋势：粉丝、互动、发布频率  
- 内容结构偏好：高表现内容的主题/结构/标题类型分布  
- 最佳发布时间建议  
- 账号定位一致性评分（内容是否偏离内容支柱）

#### 6.4.2 功能：内容数据分析（单篇/多篇）
**输入**：
- 笔记/视频列表（链接/ID/导入报表）
- 内容元数据：标题、发布时间、话题标签、封面类型（可人工补充）

**输出**：
- 内容表现排行与分层（爆款/中腰部/长尾）  
- 爆款要素归因（可解释的规则 + AI 总结）：标题/封面/结构/话题/时效性  
- 下一轮选题建议：延展角度、系列化建议  
- Prompt 优化建议（可选）：基于高表现样本，建议调整标题/开头模板的约束

---

## 7. 横切能力：提示词模板 + 文件级个性化配置

### 7.1 设计目标
1) **每个功能都有默认 Prompt**，开箱即用。  
2) 支持用户在不改代码的情况下进行定制：账号/项目/文件级覆盖。  
3) 支持 Prompt 版本管理、回滚、灰度（面向未来 A/B）。  
4) 支持“可审计”：每次生成记录使用了哪个模板与哪些变量（便于复盘与排错）。  

### 7.2 Prompt 模板模型（建议抽象）
每个模板包含：
- template_id：如 `copy.title.generate.v1`
- 场景：标题/开头/脚本/拆解/选题等
- system_prompt：系统提示词
- user_prompt：用户提示词（可含变量）
- output_schema：输出结构（JSON Schema/字段约束，用于可控输出）
- model_profile：使用的模型（文本/图片）、温度、最大 token 等
- safety_rules：敏感词与合规规则
- version：v1/v2/…

### 7.3 个性化配置（文件/账号/项目）
#### 7.3.1 配置层级与覆盖优先级
定义四层配置（从低到高优先级）：
1) **系统默认**：平台内置模板与风格（不可删除，可复制）  
2) **项目/账号配置**：基于“账号定位”沉淀的全局变量（如受众、调性、内容支柱）  
3) **用户个人配置**：用户在“配置中心”设定（风格、禁忌词、常用结构、常用 CTA）  
4) **文件级配置**：单个选题/单篇内容的临时覆盖（例如本篇要更犀利/更短/更偏故事）

最终生成时的变量合并策略：
- 同名字段：高优先级覆盖低优先级  
- 列表字段（如禁忌词、关键词）：默认合并去重；支持用户选择“覆盖/合并”策略  
- 每次生成记录“最终生效配置快照”（便于复现与排障）

#### 7.3.2 配置文件形态（满足“文件个性化配置”）
支持用户通过**上传/粘贴**配置文件来进行个性化，推荐 YAML（可读性强），同时支持 JSON：
- 文件名示例：`xhs_profile.yaml`、`content_override.yaml`
- 入口：配置中心 / 编辑器侧边栏（本篇配置）

**示例：账号/个人配置（YAML）**
```yaml
platform: xiaohongshu
persona:
  role: "护肤成分党"
  tone: ["理性", "克制", "不制造焦虑"]
audience:
  who: "25-35职场女性"
  pain_points: ["痘痘反复", "预算有限", "看不懂成分表"]
writing:
  structure: ["结论先行", "分点说明", "证据链", "总结+互动"]
  cta_style: "评论区提问"
safety:
  banned_words: ["治愈", "秒白", "医美替代"]
```

**示例：文件级覆盖（YAML）**
```yaml
override:
  tone: ["更犀利", "更口语"]
  length: "800-1200字"
  must_include: ["价格对比", "适用肤质表"]
  banned_words_add: ["闭眼入"]
```

#### 7.3.3 Prompt 覆盖与管理
支持三种使用方式：
1) **默认模板直接用**（MVP 必须具备）  
2) **复制模板 → 自定义 → 设为个人默认**  
3) **针对某篇内容临时选择模板版本**（如：标题模板 v1/v2）

模板管理能力（建议逐步上线）：
- 版本：v1/v2/…，支持回滚  
- 变量预览：查看模板依赖变量是否齐全（缺失提示）  
- 试运行：输入示例变量，预览输出  
- 权限：个人模板 vs 系统模板；（后续）团队共享模板

### 7.4 生成记录与可解释性（建议）
每次生成保存：
- 使用的功能点（标题/开头/脚本/封面等）
- 使用的 template_id + version
- 生效配置快照（合并后的变量）
- 模型参数（温度、max tokens 等）
- 输出结果与用户编辑痕迹（可选）

目的：
- 复盘：哪些模板更好用  
- 排障：为什么这次生成“跑偏”  
- 评估：后续支持 A/B 与自动推荐最佳模板

---

## 8. 数据与集成方案（概要）
### 8.0 集成原则：API 优先 + 全链路 Mock
你将提供热点榜单 API，且前期所有依赖 API 的能力（**包括大语言模型调用**）都先以 **Mock 数据** 方式联调：
- 目的：前后端并行开发、缩短集成等待时间  
- 要求：各能力抽象为稳定的“能力接口”（如 HotAPI / LLMAPI / LayoutRenderAPI / AnalyticsAPI / RAGAPI），Mock 与真实实现可一键切换  
- 生成日志需标记：本次调用来源（mock/real）、接口版本、耗时与错误码

**鉴权约定（当前）**：统一采用 **API Key**（请求头携带），便于服务端到服务端调用与快速落地。

### 8.1 外部依赖
1) **热点榜单 API（小红书优先）**：由你提供；产品侧按统一榜单接口消费（支持 Mock）  
2) **LLM 调用 API**：由你提供/统一网关；产品侧仅编排 Prompt、变量与输出结构（支持 Mock）  
3) **封面排版渲染服务**：模板化渲染为主（P0）；图片生成为可选增强（支持 Mock）  
4) **RAG 基础设施**：文件存储 + 向量检索（向量库/Embedding 服务）（支持 Mock/降级：仅关键字检索）  
5) **数据分析导入与 API**：MVP CSV/Excel 导入；后续接入平台 API 授权（支持 Mock）

### 8.2 核心数据对象（建议）
- User（用户）
- Workspace/Project（工作空间/项目）
- AccountProfile（账号定位结果 + 可编辑配置）
- BenchmarkAnalysis（拆解任务与结果）
- Topic（选题卡片）
- Draft（文案草稿：标题/开头/正文/脚本/封面）
- Asset（素材：封面图、图片等）
- AnalyticsImport（数据导入批次）
- AnalyticsReport（分析报告）
- PromptTemplate（提示词模板）
- ConfigProfile（配置档案：个人/账号/文件级）
- GenerationLog（生成日志）

---

## 9. 埋点与衡量指标（Metrics）
### 9.1 北极星指标（建议）
- **从选题到可发布初稿的平均耗时**（越低越好）  
或  
- **周活跃创作数（生成并进入编辑器的内容数）**

### 9.2 过程指标
- 功能使用渗透：定位/拆解/选题/标题/开头/脚本/封面/分析 各模块 UV、转化率  
- 生成满意度：生成后“有用/一般/无用”反馈；二次生成率  
- 编辑幅度：用户对生成结果的修改比例（越低不一定越好，需要与效果结合）  
- 留存：D1/D7/D30  
- 模型成本：人均调用次数、单次平均 token、封面生成次数

### 9.3 结果指标（需用户导入数据后才可衡量）
- 发布内容的互动率提升（点赞/收藏/评论）  
- 爆款率提升（超过阈值的内容占比）  
- 账号增长（粉丝/曝光）

---

## 10. MVP 范围（建议拆分）
### 10.1 MVP 必须有（P0）
1) 小红书账号定位：生成 + 保存为账号配置  
2) 选题生成：基于定位 + 关键词生成选题池  
3) 文案生成：标题、开头、正文填充（结构化块）  
4) 封面：先做“封面文案 + 模板排版渲染（3-6张）”，支持文字可编辑后重新渲染；首期内置 **8 套模板**  
5) Prompt & 配置中心：  
   - 系统默认模板  
   - 个人配置（风格/禁忌词/结构偏好）  
   - 文件级覆盖（本篇写作要求）  
6) 知识库（RAG 基础版）：资料上传/索引/检索；在“脚本填充”中支持可选引用与引用清单输出  
7) 生成日志（最少记录 template_id/version + 生效变量快照 + mock/real 标记）

### 10.2 MVP 可选（P1）
1) 账号拆解：粘贴内容的结构化拆解 + 模板沉淀  
2) 热点榜单：接你提供的 API 的真实联调版（从 Mock 切到 Real），支持收藏加入选题池  
3) 数据分析：先做 CSV 导入 + 基础图表 + AI 复盘建议  

### 10.3 后续增强（P2）
- 多平台深度适配（抖音/视频号/公众号的规则与模板）  
- API 授权拉取数据  
- 生图背景/风格化素材与模板渲染融合（更强视觉效果）  
- 团队协作与模板共享  
- 自动评估与自动推荐最佳模板（基于历史表现）

---

## 11. 里程碑（建议）
以 6 周为例（可按你资源调整）：
1) 第 1-2 周：账号定位 + 选题池 + 基础编辑器（标题/开头/正文）+ Mock 能力接口打通  
2) 第 3-4 周：Prompt/配置中心（个人+文件级）+ 生成日志 + 封面模板渲染（排版）  
3) 第 5 周：知识库（RAG 基础版）+ 拆解（MVP）+ 热点榜单（Mock → Real 联调）  
4) 第 6 周：数据导入与复盘（基础版）+ 稳定性/成本/监控 + 灰度上线

---

## 12. 风险与对策
1) **模型输出不稳定/跑偏** → 输出结构化 schema + 变量校验 + 安全规则 + 版本管理回滚  
2) **封面排版一致性/审美不足** → 内置模板库分风格分场景；支持品牌色/字体约束；提供“保底模板”  
3) **热点榜单 API 稳定性/字段变更** → 接口版本化；Mock/Real 双通道；关键字段缺失时降级展示  
4) **数据分析导入复杂** → MVP 只支持少量固定格式（提供模板文件下载）；逐步扩展解析能力  
5) **成本不可控** → 设定用量额度、缓存、长文分段生成、模型分层（高质/经济）
6) **RAG 引用失真/过期** → 资料版本与更新时间提示；引用片段强绑定来源；支持下线资料与重建索引

---

## 13. 待确认问题（需要你拍板）
1) API 规范剩余待定：QPS 限流、超时重试、计费计量字段、版本策略（鉴权已定：API Key）。  
2) 热点榜单 API：已确定需包含“示例内容”；剩余确认：是否支持按赛道/关键词过滤、分页与去重策略。  
3) 封面模板：首期内置 8 套模板已定；剩余确认：模板是否由你提供设计规范/素材（字体授权、品牌色、图标库）。  
4) RAG：已定仅文档类（不做OCR/表格解析）；剩余确认：引用是否必须展示来源标注（默认建议开启）。  
5) 计费方式：按月订阅 + 次数包？是否需要免费试用与水印策略？
